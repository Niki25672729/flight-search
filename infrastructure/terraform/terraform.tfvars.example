# Copy this file to terraform.tfvars (gitignored) and fill in real values.

impersonator_email = "you@example.com"

# Below should be set by tf.sh (sources .env instead — see infrastructure/terraform/tf.sh):
# project_id          = "your-gcp-project-id"
# bucket_name         = "your-bucket-name"
# bronze_gcs_prefix   = "bronze"
# silver_gcs_prefix   = "silver"
# bigquery_dataset_id = "your-bigquery-dataset"
